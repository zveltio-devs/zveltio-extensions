# Context

## SDUI migration (2026-08-21)
Branch: feat/sdui-sms-efactura
Tabs: send (recent messages + form POST /send) and templates CRUD.
Tradeoff: no stats strip; variables as JSON text; body/template both optional at schema level (API enforces one).
