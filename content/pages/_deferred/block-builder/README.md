# Block builder (Tier-3 — not wired)

Reference implementation for a future admin block canvas. **Not synced** into Studio
(`sync-extensions` only copies `studio/src/`).

Admin list CRUD for sites/pages is SDUI (`studio/schemas/pages.json`). See
[STUDIO-DEFERRED.md](../../../STUDIO-DEFERRED.md).

To revive: wire a Tier-3 route or marketplace sandbox, or extend SDUI if the model
allows.
